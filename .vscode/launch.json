{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "lldb (Debug)",
            "program": "${workspaceFolder}/build/xllmu",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
